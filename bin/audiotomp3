#!/bin/bash

if test "${1}"; then
    input="${1}"
    output="${1%.*}"
    test "${2}" && output="${2}"
    ffmpeg -i "${input}" -vn -ar 44100 -ac 2 -b:a 192k "${output}.mp3"
else
    echo "usage: `basename ${0}` <input_file> [output_file]"
fi

