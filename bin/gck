#!/bin/bash

source "${CUSTOM_BIN_DIR}/prettyprint"

info "Checking for unfinished work."

find "${WDIR}" -mindepth 1 -maxdepth 3 -type d -or -type l | while read dir; do
    if [ -d "${dir}/.git" ]; then
        gckdir "${dir}"
    fi
done

echo
success "Done."
