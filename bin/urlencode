#!/bin/bash

set -e

source "$(dirname "${0}")/shelllog.sh"

if test "${1}"; then
    parameters="${@}"
    python -c "import urllib.parse;print(urllib.parse.quote('${parameters}', safe=''))"
else
    log "usage: $(basename "${0}") <string to encode>"
fi
