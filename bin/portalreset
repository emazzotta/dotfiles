#!/bin/bash


yes | cp -rf .env /tmp/.env
yes | cp -rf .idea /tmp/.idea
git clean -dfx
cp /tmp/.env .env 
cp /tmp/.idea .idea 
yarn
npx playwright install
yarn && docker-compose down && docker-compose up -d database && yarn clean && yarn generate && yarn build && yarn dev
