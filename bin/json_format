#!/bin/bash

set -e

show_usage() {
    echo "usage: $(basename "${0}") [-c|--clipboard] <json>"
}

case "${1:-}" in
    ""|-h|--help) show_usage ;;
    -c|--clipboard) pbpaste | jq | pbcopy ;;
    *) echo "${1}" | jq | pbcopy ;;
esac
