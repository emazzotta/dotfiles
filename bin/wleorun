#!/bin/bash

set -e

args=""
for arg in "$@"; do
    args="$args $arg"
done

echo "🔄 Ensuring VM is running..."
vmup 2>/dev/null || echo "   ✓ VM already up"

echo ""
echo "🚀 Launching Leonardo on Windows VM..."
ssh wlocal "pwsh -Command \"\\\\Mac\\Home\\Projects\\private\\dotfiles\\bin\\leorun.ps1$args\""
echo "✅ Leonardo launch complete"
