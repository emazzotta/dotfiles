#!/bin/bash

if [ ! -z "${2}" ]; then
    bundle_id="${1}"
    extension="${2}"
    echo -e "nChanging [$extension] so it opens with [$bundle_id]"
    duti -s $bundle_id .$extension all
    duti -x $extension
    echo -e "n"
else
    echo "usage: `basename ${0}` <bundle id> <extension>"
fi
