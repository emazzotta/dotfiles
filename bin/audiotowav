#!/bin/bash

set -e

if test "${1}"; then
    input="${1}"
    output="${1%.*}"
    test "${2}" && output="${2}"
    ffmpeg -i "${input}" "${output}.wav"
else
    echo "usage: $(basename "${0}") <input_file> [output_file]"
fi

