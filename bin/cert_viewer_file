#!/bin/bash

set -e

if [[ "${1:-}" == "-h" ]] || [[ "${1:-}" == "--help" ]]; then
    echo "usage: $(basename "${0}") [certificate file]"
    exit 0
fi

certificate="${1:-$(pickfile pem crt cer der)}"
[ -n "${certificate}" ] || exit 1

openssl x509 -in "${certificate}" -text -noout
