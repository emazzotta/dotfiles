#!/bin/bash

set -euo pipefail

if /usr/bin/sudo -n true 2>/dev/null; then
    /usr/bin/sudo "$@"
else
    source envify MAC_SUDO_PW 2>/dev/null
    echo "$MAC_SUDO_PW" | /usr/bin/sudo -S "$@" 2>/dev/null
fi

